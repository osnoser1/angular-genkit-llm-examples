{
  "name": "backend",
  "version": "0.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --env-file-if-exists=.env.local --watch src/index.ts",
    "check-types": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "oxlint --type-aware",
    "lint:fix": "oxlint --type-aware --fix --fix-suggestions"
  },
  "prettier": {
    "singleQuote": true
  },
  "dependencies": {
    "@genkit-ai/express": "^1.22.0",
    "@genkit-ai/google-genai": "^1.22.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "genkit": "^1.22.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@repo/oxlint-config": "workspace:*",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.1",
    "@types/node": "^24.10.0",
    "typescript": "5.9.2"
  }
}
